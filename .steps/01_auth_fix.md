フェーズ1（基盤構築と認証）の作業を継続してください。現状を確認したところ、プロジェクトの初期化と依存関係のインストールは完了していますが、以下の実装が不足しています。

【不足している作業】
1. **Prismaスキーマの反映**: `.steps/00_schema.prisma` の内容を `web/prisma/schema.prisma` に反映し、`npx prisma db push` (または migrate) を実行して User テーブル等の基盤を作成してください。
2. **NextAuth.jsの設定**: マジックリンクログイン(Resend Provider)のロジックを実装してください。具体的には `src/app/api/auth/[...nextauth]/route.ts` (NextAuth v4の場合) または適切なパスに認証設定を作成してください。
3. **ログインページの作成**: `/login` (または標準の `/api/auth/signin`) でEメールを入力し、送信できる画面を作成してください。
4. **ホーム画面の保護**: `/` (page.tsx) を、ログインしていないユーザーはアクセスできない（またはログインへリダイレクトする）ように変更し、ログイン済みの場合はログアウトボタンを表示するようにしてください。

現在の `page.tsx` は初期状態のままですので、これらを実装して「ログインしてホーム画面に入れる」状態まで完了させてください。
